---
consul_run_as_server: yes
consul_retry_join:
  provider: aws
  region: us-east-1
  key: consul-servers # in TF land => ${var.consul_cluster_tag_key}
  value: terraform-consul # in TF land => ${var.consul_cluster_name}
