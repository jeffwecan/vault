/var/log/nginx/*.log {
    daily
    missingok
    rotate {{ nginx_log_retention_days }}
    compress
    delaycompress
    nocreate
    sharedscripts
    postrotate
        kill -USR1 `cat /var/run/nginx.pid` >/dev/null 2>&1 || true
    endscript
}
