#!/bin/bash

# Check that a feature flag is not disabled. This is handy for feature-flagging
# non-PHP code (scripts, cron jobs, etc). Whereas check-feature-enabled has 
# default disabled semantics, this has default enabled semantics
#
# To use:
# check-feature-not-disabled feature && do-the-thing

if [ "$#" -ne 1 ]; then
    echo "Must specify feature flag"
    exit 1
fi

# No file in disabled. 
[[ ! -f "/etc/wpengine/disabled/$1" ]]
